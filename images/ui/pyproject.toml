[project]
name = "containerops-ui"
version = "0.1.0"
description = "SRE AI Assistant UI"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
